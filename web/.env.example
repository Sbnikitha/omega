# Server-only — never use NEXT_PUBLIC_ for secrets (they ship to the browser bundle)
OMEGA_API_URL=http://localhost:8001
# x402 receipt injected by /api/omega proxy on POST /incidents and replay
OMEGA_PAYMENT_TOKEN=demo-paid
# Copilot tab — OpenUI streaming (server route only)
OPENAI_API_KEY=sk-...
