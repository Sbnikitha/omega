{
  "name": "omega-guild-commander",
  "version": "1.0.0",
  "private": true,
  "description": "Guild.ai Incident Commander — dispatches OMEGA REST pipeline",
  "scripts": {
    "test": "echo 'Run: guild agent test --ephemeral (requires Guild CLI + workspace)'"
  },
  "dependencies": {
    "@guildai/agents-sdk": "^0.1.0",
    "zod": "^3.23.0"
  }
}
